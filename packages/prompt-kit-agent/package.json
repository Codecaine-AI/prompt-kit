{
	"name": "@codecaine-ai/prompt-kit-agent",
	"version": "0.0.2",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "bun run src/server.ts",
		"start": "bun run src/server.ts",
		"test": "bun test ./test ./catalog",
		"typecheck": "bunx --bun tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"@agent-kernel/db": "workspace:*",
		"@agent-kernel/kernel": "workspace:*",
		"@codecaine-ai/prompt-kit-server": "workspace:*",
		"drizzle-orm": "^0.45.0",
		"elysia": "^1.2.0"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5"
	}
}
